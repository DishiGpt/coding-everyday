Solution { 
public: 
int rob(vector<int>& nums) { 
 int prev2 = 0; // dp[i-2]
    int prev1 = 0; // dp[i-1]

    for (int x : nums) {
        int take = prev2 + x;
        int skip = prev1;
        int curr = max(take, skip);

        prev2 = prev1;
        prev1 = curr;
    }

    return prev1;
} 
};
