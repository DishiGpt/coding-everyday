class Solution {
public:
    vector<int> recoverOrder(vector<int>& order, vector<int>& friends) {
        vector<int>res;
        for(auto ch:order){
            if(find(friends.begin(),friends.end(),ch)!=friends.end()){
                res.push_back(ch);
            }
        }
        return res;
    }
};
