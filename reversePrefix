class Solution {
public:
    string reversePrefix(string s, int k) {
        int beg=0,end=k-1;
        while(beg<end){
            char c=s[beg];
            s[beg]=s[end];
            s[end]=c;
            beg++;end--;
        }
        return s;
    }
};
