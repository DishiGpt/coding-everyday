class Solution {
public:
    string removeTrailingZeros(string num) {
        if(num.back()!='0') return num;
        while (!num.empty() && num.back() == '0') {
        num.pop_back();
        }
        return num;
    }
};
