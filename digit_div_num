class Solution {
public:
    int countDigits(int num) {
        int temp=num;
        int result=0;
        while(temp){
            int digit=temp%10;
            if(num%digit==0){
                result++;
            }
            temp=temp/10;
        }
        return result;
    }
};
