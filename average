class Solution {
public:
    double average(vector<int>& salary) {
        sort(salary.begin(),salary.end());
        double sum=0;
        int s=salary.size();
        for(int i=1;i<s-1;i++){
            sum+=salary[i];
        }
        return sum/(s-2);
    }
};
