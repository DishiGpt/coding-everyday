class Solution {
public:
    int maximizeSum(vector<int>& nums, int k) {
        sort(nums.begin(),nums.end());
        int *ptr=&nums.back();int total=0;
        while(k>0){
            total+=*ptr;
            *ptr+=1;
            k--;
        }
        
        return total;
    }
};
