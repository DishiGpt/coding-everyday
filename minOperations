class Solution {
public:
    int minOperations(vector<int>& nums, int k) {
        int sum=0;
        for(auto ch:nums){
            sum+=ch;
        }
        if(sum%k==0)return 0;
        return sum - (sum/k)*k;
    }
};
