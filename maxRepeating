class Solution {
public:
    int maxRepeating(string sequence, string word) {
        int ans=0;string current=word;
        while(sequence.find(current)!=string::npos){
            ans++;
            current+=word;
        }
        return ans;
    }
};
