class Solution {
public:
    int findNumbers(vector<int>& nums) {
        int sum=0,count=0;
        for(int num:nums){
            count=0;
            while(num){
                count++;
                num/=10;
            }
            if((count%2)==0)sum++;
        }
        return sum;
    }
};
