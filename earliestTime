class Solution {
public:
    int earliestTime(vector<vector<int>>& tasks) {
        int total=INT_MAX;
        for(int i=0;i<tasks.size();i++){
            total=min(total,tasks[i][0]+tasks[i][1]);
        }
        return total;
    }
};
