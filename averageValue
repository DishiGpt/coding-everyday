class Solution {
public:
    int averageValue(vector<int>& nums) {
        int count=0,sum=0;
        for(auto ch:nums){
            if(ch%6==0){
                sum+=ch;
                count++;
            }
        }
        if (count==0) return 0;
        return sum/count;
    }
};
